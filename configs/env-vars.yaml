# Configuration using environment variables
# Set OPENFS_DATA_ROOT before using this config

name: env-config

backends:
  local:
    type: fs
    root: ${OPENFS_DATA_ROOT}

mounts:
  - path: /data
    backend: local

# Configuration using environment variables
# Set AX_DATA_ROOT before using this config

name: env-config

backends:
  local:
    type: fs
    root: ${AX_DATA_ROOT}

mounts:
  - path: /data
    backend: local
